# Traceability Matrix — Deckle v2 Signature Imposition

Spec: `docs/specs/2026-08-04-deckle-signatures-v2.md`  ·  42 requirements  ·  13 sub-specs

`Worker Status` and `Verify Status` are updated by later pipeline stages. `Holdout?` is `no`
throughout: no holdout config (`forge/config/defaults.json`) exists in this repo, so holdout
selection was skipped.

| REQ ID | Requirement | Sub-Spec | Worker Status | Verify Status | Holdout? |
|--------|-------------|----------|---------------|---------------|----------|
| REQ-001 | `deckle/core/models.py` defines `Side` as a frozen dataclass matching contract 1. | 1 — Model changes — `Side`, `Mark`, `Signature`, `SheetPlan.signatures`, `LayoutSettings` | pending | pending | no |
| REQ-002 | `Sheet.front` and `Sheet.back` are typed `Side | None`, and an absent side is represented as `None`. | 1 — Model changes — `Side`, `Mark`, `Signature`, `SheetPlan.signatures`, `LayoutSettings` | pending | pending | no |
| REQ-002 | `Sheet.front` and `Sheet.back` are typed `Side | None`, and an absent side is represented as `None`. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-003 | Constructing `Side(pages=())` raises `ValueError` rather than producing an empty side. | 1 — Model changes — `Side`, `Mark`, `Signature`, `SheetPlan.signatures`, `LayoutSettings` | pending | pending | no |
| REQ-004 | `deckle/core/models.py` defines `Mark` as a frozen dataclass matching contract 5. | 1 — Model changes — `Side`, `Mark`, `Signature`, `SheetPlan.signatures`, `LayoutSettings` | pending | pending | no |
| REQ-005 | `deckle/core/models.py` defines `Signature` as a frozen dataclass matching contract 3. | 1 — Model changes — `Side`, `Mark`, `Signature`, `SheetPlan.signatures`, `LayoutSettings` | pending | pending | no |
| REQ-006 | `SheetPlan` gains `signatures: tuple[Signature, ...] = ()`. | 1 — Model changes — `Side`, `Mark`, `Signature`, `SheetPlan.signatures`, `LayoutSettings` | pending | pending | no |
| REQ-007 | `LayoutWarning.kind` accepts `"sheet_orientation"`, `"signature_padding"`, `"creep_advisory"` and `"landscape_imageable_unverified"` in addition to its four existing kinds. | 1 — Model changes — `Side`, `Mark`, `Signature`, `SheetPlan.signatures`, `LayoutSettings` | pending | pending | no |
| REQ-008 | `LayoutSettings` gains exactly the five committed fields with exactly the committed defaults, and no others. | 1 — Model changes — `Side`, `Mark`, `Signature`, `SheetPlan.signatures`, `LayoutSettings` | pending | pending | no |
| REQ-009 | `GutterShiftStrategy`'s emitted `Placement` values are byte-identical before and after the `Side` refactor, proven by the Pinebox golden fixture and the existing `tests/test_layout.py` suite. | 2 — The `Side` refactor across `deckle.core` — zero behaviour change | pending | pending | no |
| REQ-009 | `GutterShiftStrategy`'s emitted `Placement` values are byte-identical before and after the `Side` refactor, proven by the Pinebox golden fixture and the existing `tests/test_layout.py` suite. | 7 — Generalise the shared placement math to a `Cell` | pending | pending | no |
| REQ-009 | `GutterShiftStrategy`'s emitted `Placement` values are byte-identical before and after the `Side` refactor, proven by the Pinebox golden fixture and the existing `tests/test_layout.py` suite. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-009 | `GutterShiftStrategy`'s emitted `Placement` values are byte-identical before and after the `Side` refactor, proven by the Pinebox golden fixture and the existing `tests/test_layout.py` suite. | 12 — Integration, the end-to-end fold round trip, and the full-suite gate | pending | pending | no |
| REQ-010 | `deckle/core/export.py` creates exactly one PDF page per `Side` and places every page in `side.pages` into that PDF page. | 2 — The `Side` refactor across `deckle.core` — zero behaviour change | pending | pending | no |
| REQ-010 | `deckle/core/export.py` creates exactly one PDF page per `Side` and places every page in `side.pages` into that PDF page. | 12 — Integration, the end-to-end fold round trip, and the full-suite gate | pending | pending | no |
| REQ-011 | `deckle/core/render.py` continues to rasterise the exported artifact and never re-draws from `Placement`, with the front-then-back page-index mapping preserved. | 2 — The `Side` refactor across `deckle.core` — zero behaviour change | pending | pending | no |
| REQ-011 | `deckle/core/render.py` continues to rasterise the exported artifact and never re-draws from `Placement`, with the front-then-back page-index mapping preserved. | 3 — The `Side` refactor in `deckle.app` — per-page clipping is a correctness fix | pending | pending | no |
| REQ-012 | `deckle/app/backend.py` paints every page on a side, not only the first. | 3 — The `Side` refactor in `deckle.app` — per-page clipping is a correctness fix | pending | pending | no |
| REQ-013 | `deckle/app/views/preview_view.py` computes clipping warnings **per page within a side**, not per side. | 3 — The `Side` refactor in `deckle.app` — per-page clipping is a correctness fix | pending | pending | no |
| REQ-014 | `deckle/core/print_session.py::_hash_plan` includes each side's `source_ref.page_index` tuple, so two different page orderings over the same sheet count hash differently. | 4 — `_hash_plan` content-awareness, and the zero-diff seam assertion | pending | pending | no |
| REQ-015 | `deckle/core/printing.py` and `deckle/core/profiles.py` are unchanged, asserted by a test rather than by inspection. | 4 — `_hash_plan` content-awareness, and the zero-diff seam assertion | pending | pending | no |
| REQ-015 | `deckle/core/printing.py` and `deckle/core/profiles.py` are unchanged, asserted by a test rather than by inspection. | 12 — Integration, the end-to-end fold round trip, and the full-suite gate | pending | pending | no |
| REQ-016 | `split_signatures` returns contiguous, gapless, non-overlapping sheet-index groups covering every sheet exactly once, with the remainder in the final group. | 5 — `deckle/core/signatures.py` — the arithmetic, and the fold simulator | pending | pending | no |
| REQ-016 | `split_signatures` returns contiguous, gapless, non-overlapping sheet-index groups covering every sheet exactly once, with the remainder in the final group. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-017 | `saddle_order(n)` is a permutation of `range(n)` for every multiple of 4 up to 128, and `saddle_order(8) == [7, 0, 1, 6, 5, 2, 3, 4]`. | 5 — `deckle/core/signatures.py` — the arithmetic, and the fold simulator | pending | pending | no |
| REQ-018 | `fold_reading_order` is implemented from the physical fold description and contains no call to `saddle_order`. | 5 — `deckle/core/signatures.py` — the arithmetic, and the fold simulator | pending | pending | no |
| REQ-019 | `fold_reading_order(impose(pages, settings))` reproduces the source page order for every combination of page count {1…40, 100, 266} × sheets-per-signature {1…8} × binding edge {left, right}. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-019 | `fold_reading_order(impose(pages, settings))` reproduces the source page order for every combination of page count {1…40, 100, 266} × sheets-per-signature {1…8} × binding edge {left, right}. | 12 — Integration, the end-to-end fold round trip, and the full-suite gate | pending | pending | no |
| REQ-020 | `document_scale`, `content_box_size`, `content_box_rect_pt` and `actual_margins_pt` each accept a `Cell`, and `GutterShiftStrategy` passes the full sheet as its cell. | 7 — Generalise the shared placement math to a `Cell` | pending | pending | no |
| REQ-021 | `SaddleStitchStrategy` implements `LayoutStrategy` with the unmodified signature `impose(self, pages: Sequence[SourcePage], settings: LayoutSettings) -> SheetPlan`. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-021 | `SaddleStitchStrategy` implements `LayoutStrategy` with the unmodified signature `impose(self, pages: Sequence[SourcePage], settings: LayoutSettings) -> SheetPlan`. | 11 — UI surface — Binding group, per-cell preview, signature print selector | pending | pending | no |
| REQ-021 | `SaddleStitchStrategy` implements `LayoutStrategy` with the unmodified signature `impose(self, pages: Sequence[SourcePage], settings: LayoutSettings) -> SheetPlan`. | 12 — Integration, the end-to-end fold round trip, and the full-suite gate | pending | pending | no |
| REQ-022 | A 266-page document at `sheets_per_signature=4` produces 17 signatures, 67 sheets, and exactly 2 blanks, all in the final signature. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-023 | Across all 67 sheets of the 266-page fixture there is exactly one distinct non-filler `scale_x` value. | 7 — Generalise the shared placement math to a `Cell` | pending | pending | no |
| REQ-023 | Across all 67 sheets of the 266-page fixture there is exactly one distinct non-filler `scale_x` value. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-024 | Padding runs exactly one pass and emits exactly one `signature_padding` warning naming the blank count. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-025 | Under `fold_scheme="folio"` a leaf's spine edge is derived from its cell's position relative to the fold — left cell's spine on its right edge, right cell's spine on its left — never from output-page parity. | 7 — Generalise the shared placement math to a `Cell` | pending | pending | no |
| REQ-025 | Under `fold_scheme="folio"` a leaf's spine edge is derived from its cell's position relative to the fold — left cell's spine on its right edge, right cell's spine on its left — never from output-page parity. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-026 | Portrait paper under `fold_scheme="folio"` emits a `sheet_orientation` warning, does not rotate the paper, and does not block. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-027 | `paper_thickness_pt` never reaches placement geometry; it appears only in the creep-advisory path, enforced mechanically. | 6 — `deckle/core/marks.py` — bindery marks as pure geometry | pending | pending | no |
| REQ-027 | `paper_thickness_pt` never reaches placement geometry; it appears only in the creep-advisory path, enforced mechanically. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-028 | `sewing_stations(...)` returns `settings.sewing_stations` `Mark`s of kind `sewing_station`, evenly spaced between a head and tail inset, centred on the fold line. | 6 — `deckle/core/marks.py` — bindery marks as pure geometry | pending | pending | no |
| REQ-028 | `sewing_stations(...)` returns `settings.sewing_stations` `Mark`s of kind `sewing_station`, evenly spaced between a head and tail inset, centred on the fold line. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-029 | `signature_order_mark(...)` steps monotonically down the spine across signature indices, with the first and last at the stated insets. | 6 — `deckle/core/marks.py` — bindery marks as pure geometry | pending | pending | no |
| REQ-029 | `signature_order_mark(...)` steps monotonically down the spine across signature indices, with the first and last at the stated insets. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-029 | `signature_order_mark(...)` steps monotonically down the spine across signature indices, with the first and last at the stated insets. | 13 — The physical folded dummy — `[HUMAN REVIEW]`, and the gate | pending | pending | no |
| REQ-030 | `fold_line(...)` returns one `Mark` of kind `fold_line` lying on the cell boundary. | 6 — `deckle/core/marks.py` — bindery marks as pure geometry | pending | pending | no |
| REQ-030 | `fold_line(...)` returns one `Mark` of kind `fold_line` lying on the cell boundary. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-031 | Marks are drawn in `deckle/core/export.py` with `pikepdf.canvas.ContentStreamBuilder`; no text is drawn, `add_overlay` is not used, and `Canvas.draw_image` is not used. | 9 — Mark rendering in `export.py` via `pikepdf.canvas.ContentStreamBuilder` | pending | pending | no |
| REQ-031 | Marks are drawn in `deckle/core/export.py` with `pikepdf.canvas.ContentStreamBuilder`; no text is drawn, `add_overlay` is not used, and `Canvas.draw_image` is not used. | 12 — Integration, the end-to-end fold round trip, and the full-suite gate | pending | pending | no |
| REQ-032 | An exported folio sheet's coalesced content stream contains exactly two balanced `q…Q` blocks per PDF page, each a pure `1 0 0 1 tx ty cm` translation when the document scale is 1.0. | 9 — Mark rendering in `export.py` via `pikepdf.canvas.ContentStreamBuilder` | pending | pending | no |
| REQ-032 | An exported folio sheet's coalesced content stream contains exactly two balanced `q…Q` blocks per PDF page, each a pure `1 0 0 1 tx ty cm` translation when the document scale is 1.0. | 12 — Integration, the end-to-end fold round trip, and the full-suite gate | pending | pending | no |
| REQ-033 | `tests/test_license_audit.py` fails if `pdfimpose`, `cpdf`, `pymupdf` or `fitz` is present in the dependency closure. | 10 — License-audit denylist, and the pdfimpose oracle that cannot become a dependency | pending | pending | no |
| REQ-033 | `tests/test_license_audit.py` fails if `pdfimpose`, `cpdf`, `pymupdf` or `fitz` is present in the dependency closure. | 12 — Integration, the end-to-end fold round trip, and the full-suite gate | pending | pending | no |
| REQ-034 | `tools/oracle_diff.py` exists outside `tests/`, is absent from `pyproject.toml`, and is imported by no shipped or tested module. | 10 — License-audit denylist, and the pdfimpose oracle that cannot become a dependency | pending | pending | no |
| REQ-035 | `plan_passes(plan, profile, sheets=sig.sheet_indices)` returns passes covering only that signature, with no new branch in `printing.py`. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-035 | `plan_passes(plan, profile, sheets=sig.sheet_indices)` returns passes covering only that signature, with no new branch in `printing.py`. | 11 — UI surface — Binding group, per-cell preview, signature print selector | pending | pending | no |
| REQ-035 | `plan_passes(plan, profile, sheets=sig.sheet_indices)` returns passes covering only that signature, with no new branch in `printing.py`. | 12 — Integration, the end-to-end fold round trip, and the full-suite gate | pending | pending | no |
| REQ-036 | `deckle/app/views/layout_panel.py` exposes a Binding group (fold scheme, sheets per signature, blank mode, sewing stations, paper thickness) with a live "N signatures · M sheets · K blanks" readout. | 11 — UI surface — Binding group, per-cell preview, signature print selector | pending | pending | no |
| REQ-037 | Under folio, `preview_view` labels each cell with its source page number and the sheet's signature index, and draws the content-box guide **per cell**. | 11 — UI surface — Binding group, per-cell preview, signature print selector | pending | pending | no |
| REQ-038 | `deckle/app/views/print_dialog.py` gains a signature selector that populates the existing `sheets=` argument, adding no pass-ordering or sheet-cursor arithmetic. | 11 — UI surface — Binding group, per-cell preview, signature print selector | pending | pending | no |
| REQ-039 | `python -m pytest -q` reports zero failures with no fewer than 237 tests collected, and `ruff check deckle tests` exits 0. | 12 — Integration, the end-to-end fold round trip, and the full-suite gate | pending | pending | no |
| REQ-040 | No AGPL dependency, no external runtime binary, no Qt import in `deckle.core`, and no file/network/print I/O in `deckle/core/layout.py`. | 2 — The `Side` refactor across `deckle.core` — zero behaviour change | pending | pending | no |
| REQ-040 | No AGPL dependency, no external runtime binary, no Qt import in `deckle.core`, and no file/network/print I/O in `deckle/core/layout.py`. | 3 — The `Side` refactor in `deckle.app` — per-page clipping is a correctness fix | pending | pending | no |
| REQ-040 | No AGPL dependency, no external runtime binary, no Qt import in `deckle.core`, and no file/network/print I/O in `deckle/core/layout.py`. | 7 — Generalise the shared placement math to a `Cell` | pending | pending | no |
| REQ-040 | No AGPL dependency, no external runtime binary, no Qt import in `deckle.core`, and no file/network/print I/O in `deckle/core/layout.py`. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
| REQ-040 | No AGPL dependency, no external runtime binary, no Qt import in `deckle.core`, and no file/network/print I/O in `deckle/core/layout.py`. | 10 — License-audit denylist, and the pdfimpose oracle that cannot become a dependency | pending | pending | no |
| REQ-040 | No AGPL dependency, no external runtime binary, no Qt import in `deckle.core`, and no file/network/print I/O in `deckle/core/layout.py`. | 12 — Integration, the end-to-end fold round trip, and the full-suite gate | pending | pending | no |
| REQ-041 | A physical folded dummy — one 4-sheet signature printed through the real calibrated `PrinterProfile`, folded, nested — reads front to back in correct page order, and the sewing stations land where an awl wants them. | 13 — The physical folded dummy — `[HUMAN REVIEW]`, and the gate | pending | pending | no |
| REQ-042 | Under folio, `clipped_by_page` is measured against the **cell**, not the sheet. | 8 — `SaddleStitchStrategy` — folio 2-up imposition | pending | pending | no |
